# Cloudflare Pages / Workers `_redirects` note:
# - Destinations must be relative URLs (no `https://...`).
# - Only these status codes are allowed: 200, 301, 302, 303, 307, 308.
# - Host-level redirects (www → apex) and HTTP → HTTPS should be configured in Cloudflare
#   (Redirect Rules / Bulk Redirects / Always Use HTTPS), not here.

